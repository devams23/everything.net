## Encapsulation (Properties, Get/Set, Readonly)
## Abstraction & Interfaces
## Interfaces & Multiple Inheritance


### In C#, the default access level for an interface declaration (when defined directly within a namespace) is internal, while the default access level for members within an interface (methods, properties, etc.) is public